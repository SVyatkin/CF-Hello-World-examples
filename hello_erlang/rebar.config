{sub_dirs, [ "rel" ]}.

{erl_opts, [debug_info]}. %, fail_on_warning]}.

{require_otp_vsn, "17"}.

{deps_dir, ["deps"]}.

{deps,
  [
    {cowboy, ".*", {git, "https://github.com/extend/cowboy.git", {branch, "master"}}}
  ]
}.
